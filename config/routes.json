{
  "routes": [
    {
      "method": "GET",
      "path": "/",
      "handler": "revision.index",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/collection-entries",
      "handler": "revision.collectionEntries",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/config",
      "handler": "revision.config",
      "config": {
        "policies": []
      }
    }
  ]
}
