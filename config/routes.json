{
  "routes": [
    {
      "method": "GET",
      "path": "/",
      "handler": "revision.index",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/config",
      "handler": "revision.config",
      "config": {
        "policies": []
      }
    }
  ]
}
